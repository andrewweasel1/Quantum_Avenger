# Decoupled dashboard API (FastAPI layer over the ML engine).
# NOT in the SessionStart hook's requirements.txt — install on the host that
# serves the dashboard:  pip install -r new_pipeline/requirements-api.txt
fastapi>=0.110
uvicorn>=0.27
# httpx is pulled in transitively (Starlette TestClient) and used by the tests.
